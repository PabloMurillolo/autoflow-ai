# v0.1 runs without any keys. These placeholders are for a future SERVER ONLY.
# Never prefix secrets with VITE_: Vite exposes those values to browsers.
OPENAI_API_KEY=
HF_API_KEY_ID=
HF_API_KEY_SECRET=
CALENDAR_CLIENT_ID=
CALENDAR_CLIENT_SECRET=
SMS_API_KEY=
CRM_API_KEY=
DATABASE_URL=
